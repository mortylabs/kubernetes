# ⚠️  This file is listed in .gitignore — never commit real credentials
# Create the namespace first: kubectl create namespace wordpress
apiVersion: v1
kind: Secret
metadata:
  name: mariadb-pass
  namespace: wordpress
  labels:
    app: wordpress
type: Opaque
stringData:
  password: CHANGEME_STRONG_PASSWORD
